const fs = require('fs');
const path = require('path');

const dbController = {};

dbController.getDatabase = (req, res, next) => {
    try {

    } catch (e) {

    }
}


module.exports = dbController;